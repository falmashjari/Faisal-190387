<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.faisal_190387">
<uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.Faisal190387">
        <activity android:name=".WeatherMainActivity"></activity>
        <activity android:name=".ShowUserInSqliteActivity" />
        <activity android:name=".AddUserSqliteActivity" />
        <activity android:name=".SqliteMainActivity" />
        <activity android:name=".ShowUserInFirebaseActivity" />
        <activity android:name=".AddUserInFirebaseActivity" />
        <activity android:name=".FirebaseMainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>